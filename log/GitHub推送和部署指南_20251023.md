# GitHub推送和部署完整指南

**创建日期**: 2025-10-23
**项目**: 宇硕复盘系统
**问题**: Git push 网络连接被重置

---

## 一、当前状态

### Git提交状态
✅ 本地提交已完成：
```
commit 6496dee - docs: 添加功能验证报告和项目文档更新
- 新增功能验证报告
- 更新项目文档和部署脚本
- 所有功能均已实现
```

### 待推送内容
- 共38个提交待推送到远程main分支
- 8个文件修改，480行新增内容
- 包含完整的功能验证报告

### 遇到的问题
```
fatal: unable to access 'https://github.com/yushuo1991/limit-up-analysis.git/':
Recv failure: Connection was reset
```

---

## 二、网络问题解决方案

### 方案1: 使用代理推送 (推荐)

如果你有代理服务器（如V2Ray、Clash等）：

```bash
# 假设代理地址为 127.0.0.1:7890
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 推送
git push origin main

# 推送后取消代理（可选）
git config --global --unset http.proxy
git config --global --unset https.proxy
```

### 方案2: 切换到SSH方式

```bash
# 1. 检查是否有SSH密钥
ls ~/.ssh/id_rsa.pub

# 2. 如果没有，生成SSH密钥
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# 3. 查看公钥并复制
cat ~/.ssh/id_rsa.pub

# 4. 添加到GitHub
# 访问 https://github.com/settings/keys
# 点击 "New SSH key"，粘贴公钥内容

# 5. 修改远程仓库URL
git remote set-url origin git@github.com:yushuo1991/limit-up-analysis.git

# 6. 推送
git push origin main
```

### 方案3: 使用GitHub Desktop (最简单)

1. 下载安装 **GitHub Desktop**: https://desktop.github.com/
2. 打开GitHub Desktop，File → Add Local Repository
3. 选择项目文件夹: `C:\Users\yushu\Desktop\fupan10-22`
4. 点击 "Push origin" 按钮即可推送

### 方案4: 使用GitHub CLI

```bash
# 1. 安装GitHub CLI
# 访问 https://cli.github.com/ 下载安装

# 2. 登录
gh auth login

# 3. 推送
git push origin main
```

### 方案5: 修改Git网络配置

```bash
# 增加超时时间
git config --global http.lowSpeedLimit 0
git config --global http.lowSpeedTime 999999

# 禁用SSL验证（不推荐，仅临时使用）
git config --global http.sslVerify false

# 推送
git push origin main

# 恢复SSL验证
git config --global http.sslVerify true
```

### 方案6: 分批推送

如果是提交太多导致的问题：

```bash
# 查看待推送的提交
git log origin/main..HEAD --oneline

# 分批推送（每次推送部分提交）
git push origin main~10:main  # 先推送前面的提交
git push origin main          # 再推送剩余的
```

---

## 三、手动推送步骤（推荐使用方案1或3）

### 使用代理（如果有的话）

```bash
# 1. 启动你的代理软件（如Clash、V2Ray等）

# 2. 查看代理端口（通常是7890或1080）
# 在代理软件中查看HTTP代理端口

# 3. 设置Git代理
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 4. 推送
git push origin main

# 5. 查看推送结果
git log origin/main..HEAD --oneline
```

### 不使用代理（推荐GitHub Desktop）

直接使用GitHub Desktop图形界面推送，避免命令行网络问题。

---

## 四、推送成功后的部署流程

### 自动部署配置

你的项目已经配置了自动部署，推送到GitHub后会自动触发：

#### Netlify自动部署

**配置文件**: `netlify.toml`

```toml
[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
```

**部署步骤**:
1. ✅ 推送代码到GitHub
2. ✅ Netlify自动检测到push事件
3. ✅ 自动执行 `npm run build`
4. ✅ 部署 `dist` 目录到CDN
5. ✅ 部署完成，自动更新网站

**查看部署状态**:
- 访问: https://app.netlify.com/
- 登录你的账号
- 找到项目 "宇硕复盘系统"
- 查看 "Deploys" 标签页

#### Vercel自动部署（如果使用）

**配置文件**: `vercel.json`

```json
{
  "name": "yushuo-fuplan-system",
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ]
}
```

**部署步骤**:
1. 访问: https://vercel.com/dashboard
2. 找到项目并查看部署状态
3. 每次push会自动触发新部署

---

## 五、验证部署成功

### 检查清单

1. **GitHub推送验证**:
```bash
# 查看远程分支状态
git fetch origin
git log origin/main --oneline -5

# 应该看到最新的提交: 6496dee docs: 添加功能验证报告和项目文档更新
```

2. **Netlify部署验证**:
- 访问你的Netlify网站URL
- 检查首页"盘面回顾"数据是否显示
- 检查"板块节奏"页面是否嵌入了bk.yuhsuo.click
- 测试用户登录和表单提交功能

3. **API功能验证**:
```bash
# 测试涨停数据API
curl https://你的域名/api/fetch-data?date=20250909
```

4. **数据库连接验证**:
- 登录管理后台: `/admin`
- 查看数据看板: `/admin/data-dashboard`
- 验证Supabase数据库连接正常

---

## 六、当前部署架构

```
GitHub仓库 (代码托管)
    ↓ (push触发webhook)
    ├─→ Netlify (前端部署)
    │   ├─ 自动构建: npm run build
    │   ├─ 部署静态文件: dist/
    │   ├─ Serverless Functions: netlify/functions/
    │   └─ 环境变量: Supabase配置
    │
    └─→ Vercel (备用方案)
        ├─ 前端部署: dist/
        └─ API Functions: api/

Supabase (数据库和认证)
    ├─ PostgreSQL数据库
    ├─ Row Level Security (RLS)
    ├─ Authentication服务
    └─ 5个核心数据表
```

---

## 七、环境变量配置

### Netlify环境变量

已在 `netlify.toml` 中配置：

```toml
[context.production.environment]
  VITE_SUPABASE_URL = "https://wmwcnnjvdbicxiculumk.supabase.co"
  VITE_SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  VITE_ADMIN_USERNAME = "admin"
  VITE_ADMIN_PASSWORD = "7287843"
  VITE_FAKE_EMAIL_DOMAIN = "wx.local"
```

### 手动添加环境变量（如果需要）

1. 登录Netlify Dashboard
2. 进入项目 → Site settings → Environment variables
3. 添加以下变量：
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
   - `VITE_ADMIN_USERNAME`
   - `VITE_ADMIN_PASSWORD`
   - `VITE_FAKE_EMAIL_DOMAIN`

---

## 八、Tushare API配置

### API Token配置

根据项目需求，Tushare API已在后端配置：

**Token**: `2876ea85cb005fb5fa17c809a98174f2d5aae8b1f830110a5ead6211`
**积分**: 5000分

**使用的接口**:
- `limit_list_d` - 涨停板数据 (当前使用)
- `stk_limit` - 个股涨跌停
- `limit_list` - 涨跌停统计
- `stk_factor` - 股票因子
- `moneyflow` - 资金流向
- `stk_surv` - 监控日报

### 配置位置

文件: `api/fetch-data.js`

```javascript
// 如需更新Token，修改此处
const TUSHARE_TOKEN = "2876ea85cb005fb5fa17c809a98174f2d5aae8b1f830110a5ead6211";
```

---

## 九、常见问题解决

### Q1: 推送失败 "Connection was reset"

**原因**: 网络防火墙、GFW拦截、代理问题

**解决**:
1. 使用代理（方案1）
2. 使用GitHub Desktop（方案3）
3. 切换到SSH（方案2）

### Q2: 部署后页面空白

**原因**: 构建失败或路由配置问题

**解决**:
1. 检查Netlify构建日志
2. 验证 `netlify.toml` 配置
3. 检查环境变量是否正确

### Q3: API调用失败

**原因**: Supabase连接问题或环境变量缺失

**解决**:
1. 检查Netlify环境变量配置
2. 验证Supabase URL和Key
3. 查看浏览器控制台错误

### Q4: 数据库连接失败

**原因**: RLS策略或网络问题

**解决**:
1. 登录Supabase Dashboard
2. 检查RLS策略是否启用
3. 验证用户权限配置

---

## 十、推荐操作流程

### 🎯 最简单的方案（推荐）

1. **下载GitHub Desktop**:
   - 访问: https://desktop.github.com/
   - 安装后登录GitHub账号

2. **添加本地仓库**:
   - File → Add Local Repository
   - 选择: `C:\Users\yushu\Desktop\fupan10-22`

3. **推送代码**:
   - 点击右上角 "Push origin" 按钮
   - 等待推送完成

4. **验证部署**:
   - 访问Netlify Dashboard查看部署状态
   - 访问网站验证功能

### 🔧 使用代理的方案

```bash
# 1. 启动代理软件（如Clash）

# 2. 设置Git代理（假设端口7890）
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 3. 推送
git push origin main

# 4. 取消代理
git config --global --unset http.proxy
git config --global --unset https.proxy
```

---

## 十一、部署验证清单

推送成功后，按以下清单验证部署：

- [ ] GitHub仓库显示最新提交 `6496dee`
- [ ] Netlify部署成功（绿色对勾）
- [ ] 网站首页可以正常访问
- [ ] "盘面回顾"显示真实API数据
- [ ] "板块节奏"页面嵌入了bk.yuhsuo.click
- [ ] 用户登录功能正常
- [ ] 用户表单提交功能正常
- [ ] 管理后台可以访问 `/admin`
- [ ] 数据看板可以访问 `/admin/data-dashboard`
- [ ] Supabase数据库连接正常

---

## 十二、联系方式和支持

### GitHub仓库
- URL: https://github.com/yushuo1991/limit-up-analysis
- 分支: main
- 提交数: 38 commits ahead

### Supabase项目
- URL: https://wmwcnnjvdbicxiculumk.supabase.co
- Dashboard: https://supabase.com/dashboard/project/wmwcnnjvdbicxiculumk

### Netlify项目
- Dashboard: https://app.netlify.com/

---

## 附录: Git配置备份

当前Git配置：

```bash
# 远程仓库
remote.origin.url = https://github.com/yushuo1991/limit-up-analysis.git

# HTTP配置
http.sslbackend = schannel
http.postbuffer = 524288000
http.sslverify = true
http.version = HTTP/2
http.lowspeedlimit = 0
http.lowspeedtime = 999999
```

---

**生成时间**: 2025-10-23
**生成工具**: Claude Code
**状态**: ✅ 本地提交完成，等待推送到GitHub

---

## 下一步操作

1. ✅ 选择上述任一方案推送代码到GitHub
2. ✅ 等待Netlify自动部署（约2-5分钟）
3. ✅ 访问网站验证功能
4. ✅ 查看部署日志确认无误

**推荐**: 使用 **GitHub Desktop**（方案3）最简单可靠！
